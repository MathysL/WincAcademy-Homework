function eoReporter(evenOdd) {
    for (let i = 0; i <= 20; i++) {
        console.log(`Is ${i} even or odd?`);
        // program to check if the number is even or odd
        // take input from the user
        //       const number = prompt("Enter a number: ");
        //check if the number is even (even is deelbaar en laat nul over)
        evenOdd = "even";
        if (number % 2 == 0) { console.log("The number is even."); } else
        {
            console.log("The number is odd.");
           let evenOdd = "on-even";}
    }
}