console.log("calc active")
//calc
function square(num) {
    return num * num;
}
console.log(square(num));
console.log("Hello" + (myName) + " the square is:" + square(2));
console.log("calc closing...");
//
// num-(ex)pressions
const double = function (number) {
    return number * 2;
};
double(num);//of waarde
// (double(double(5)); mv in een regel